"use client";

interface UsuarioProps {
    id: string;
}

export default function Usuario({ id }: UsuarioProps) {

    

    return (
        <div>
            <h1>Usuario</h1>
        </div>
    );
}